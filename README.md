# StereoBoy

`make build`
to build

edit makefile to have rp_path be path to mounted folder, run
`make flash`
to flash usb

`make report`
to generate code on the progress you have made

plz run cmake only in build folder